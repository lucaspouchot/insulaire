[package]
name = "hex-engine"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
description = "Engine facade: content registry, command dispatch and compact snapshots for host applications."
keywords.workspace = true
categories.workspace = true

[lints.rust]
missing_docs = "warn"

[dependencies]
hex-simulation = { workspace = true }
hex-world = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
hex-world = { workspace = true, features = ["testing"] }
