/**
 * Build features of the **client** delivery.
 *
 * Substituted for `build-features.ts` by the `deliver` build configuration; see
 * that file for why the seam exists.
 */
import { BuildFeatures } from './build-features.model';

export const BUILD_FEATURES: BuildFeatures = {
  editor: false,
  label: '',
};
